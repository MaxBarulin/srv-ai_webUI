fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
bcrypt==4.2.1
python-multipart==0.0.20
aiosqlite==0.20.0
# §16 — загрузка и парсинг документов
python-docx==1.2.0
openpyxl==3.1.5
pypdf==6.14.2
Pillow==12.3.0
# растеризация PDF в режиме «как картинку» (vision): PDFium в самом wheel —
# системный poppler НЕ нужен, ставится офлайн и без root
pypdfium2==5.12.1
# §13 — фильтр персональных данных (NER для ФИО, CPU, офлайн; модель в pip-пакетах)
natasha==1.6.0
slovnet==0.6.0
navec==0.10.0
# опциональный резерв (НЕ обязателен — по умолчанию растеризует pypdfium2):
#   poppler-utils (pdftoppm) — используется, только если pypdfium2 не установлен

# Опционально: шифрование БД (DB_KEY в .env) — раскомментировать при включении
# sqlcipher3-binary==0.5.4
